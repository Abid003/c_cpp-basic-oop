#include<stdio.h>
int main()
{
    int Radius, Area;
    printf("Enter the Value of Radius = ");
    scanf("%d.2 Meter", &Radius);
    Area = 4 * 3.1416 * Radius * Radius;
    printf("The Area is, %d Squire Meter", Area);
    return 0;
}
